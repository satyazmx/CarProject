// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseconfig : {
    apiKey: "AIzaSyAbS8S1Egw8j0hM4KIAMSChd5ZHxa2u66c",
    authDomain: "materialize-d61a9.firebaseapp.com",
    databaseURL: "https://materialize-d61a9.firebaseio.com",
    projectId: "materialize-d61a9",
    storageBucket: "materialize-d61a9.appspot.com",
    messagingSenderId: "30444420143"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
